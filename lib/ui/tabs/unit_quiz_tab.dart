import 'package:flutter/material.dart';

class UnitQuizTab extends StatelessWidget {
  const UnitQuizTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Unit Quiz Tab"),
    );
  }
}
